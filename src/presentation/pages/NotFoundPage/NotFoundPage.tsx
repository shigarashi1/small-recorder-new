import React from 'react';

import styles from './NotFoundPage.module.scss';

type TProps = {};

const NotFoundPage: React.FC<TProps> = () => {
  return <div className={styles.root}>NotFoundPage</div>;
};

export default NotFoundPage;
